{
	"home": [
		"name === 'home'",
    "visible === 'about' && winScrollY < winHeight - 200 && scrollDist >= 25"
	],
	"about": [
		"name === 'about'",
    "visible === 'skills' && winScrollY < winHeight * 2 - 200 && scrollDist >= 25",
    "visible === 'home' && winScrollY > 200 && scrollDist <= -25"
	],
	"skills": [
		"name === 'skills'",
    "visible === 'contact' && winScrollY < winHeight * 3 - 200 && scrollDist >= 25",
    "visible === 'about' && winScrollY > winHeight + 200 && scrollDist <= -25"
	],
	"contact": [
		"name === 'contact'",
    "visible === 'skills' && winScrollY > winHeight * 2 + 200 && scrollDist <= -25"
	]
}